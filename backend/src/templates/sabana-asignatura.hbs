<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <title>Sábana de Asignatura</title>
    <style>
        @page {
            size: letter;
            margin: 6mm 12mm;
        }

        body {
            font-family: 'Segoe UI', Helvetica, Arial, sans-serif;
            font-size: 10px;
            margin: 0;
            color: #334155;
            -webkit-print-color-adjust: exact;
        }

        /* Encabezado Moderno Asimétrico */
        .header-wrapper {
            border-bottom: 1px solid #2563eb;
            padding-bottom: 6px;
            margin-bottom: 14px;
        }

        .header-table {
            width: 100%;
            border-collapse: collapse;
            border: none;
        }

        .header-table td {
            border: none;
            vertical-align: middle;
        }

        .inst-title {
            margin: 0;
            font-size: 15px;
            color: #1e3a8a;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            font-weight: 800;
        }

        .inst-subtitle {
            margin: 2px 0 0 0;
            font-size: 10px;
            color: #64748b;
        }

        .report-badge {
            background-color: #eff6ff;
            border: 1px solid #bfdbfe;
            padding: 5px 12px;
            border-radius: 6px;
            text-align: right;
            float: right;
        }

        .report-badge h2 {
            margin: 0;
            font-size: 12px;
            color: #1d4ed8;
            text-transform: uppercase;
        }

        .report-badge p {
            margin: 2px 0 0 0;
            font-size: 10px;
            color: #3b82f6;
            font-weight: bold;
        }

        /* Meta Información */
        .meta-container {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
            font-size: 10px;
        }

        .meta-item {
            display: inline-block;
        }

        .meta-label {
            color: #64748b;
            font-weight: bold;
        }

        .meta-value {
            color: #0f172a;
            font-weight: 600;
            border-bottom: 1px dashed #cbd5e1;
            padding-bottom: 1px;
        }

        /* Tabla Principal Moderna */
        .data-table {
            width: 100%;
            border-collapse: collapse;
            font-size: 10px;
            line-height: 1.1;
            /* Un poco más de interlineado */
        }

        .data-table th,
        .data-table td {
            border: 1px solid #475569;
            padding: 2px 3px;
            text-align: center;
        }

        .data-table th {
            background-color: #f1f5f9;
            color: #0d0d0e;
            font-weight: 700;
            text-transform: uppercase;
        }

        /* Ajuste de anchos para formato Vertical (Ancho más reducido) */
        .col-nro {
            width: 4%;
            color: #64748b;
            font-weight: bold;
        }

        .col-nombre {
            width: 48%;
            text-align: left !important;
            padding-left: 6px !important;
            font-weight: 600;
            color: #0f172a;
            white-space: normal;
            /* Permitimos que baje a dos líneas si el nombre es extremo */
        }

        .col-nota {
            width: 8%;
            /* 6 columnas de 8% = 48% restante */
        }

        /* Reglas de Paginación para Puppeteer */
        .data-table thead {
            display: table-header-group;
        }

        .data-table tbody {
            display: table-row-group;
        }

        .data-table tbody tr {
            break-inside: avoid;
            page-break-inside: avoid;
        }

        /* Filas intercaladas */
        .data-table tbody tr:nth-child(even) {
            background-color: #f8fafc;
        }
    </style>
</head>

<body>

    <div class="header-wrapper">
        <table class="header-table">
            <tr>
                <td style="width: 60px;">
                    {{#if urlEscudo}}
                    <img src="{{urlEscudo}}" alt="Logo" style="max-height: 45px; max-width: 45px;">
                    {{/if}}
                </td>
                <td style="padding-left: 10px;">
                    <h1 class="inst-title">{{nombreInstitucion}}</h1>
                    <p class="inst-subtitle">Sede: <strong>{{sedeNombre}}</strong> | Año Lectivo:
                        <strong>{{anioLectivo}}</strong> | Jornada: <strong>{{jornada}}</strong>
                    </p>
                </td>
                <td>
                    <div class="report-badge">
                        <h2>Reporte de Asignatura</h2>
                        <p>{{areaAsignatura}}</p>
                    </div>
                </td>
            </tr>
        </table>
    </div>

    <div class="meta-container">
        <div class="meta-item"><span class="meta-label">Grado:</span> <span class="meta-value">{{gradoNombre}}</span>
        </div>
        <div class="meta-item"><span class="meta-label">Grupo:</span> <span class="meta-value">{{grupoNombre}}</span>
        </div>
        <div class="meta-item"><span class="meta-label">Docente Asignado:</span> <span
                class="meta-value">{{docenteNombre}}</span></div>
    </div>

    <table class="data-table">
        <thead>
            <tr>
                <th class="col-nro">#</th>
                <th class="col-nombre">Estudiante</th>
                <th class="col-nota">Periodo 1</th>
                <th class="col-nota">Periodo 2</th>
                <th class="col-nota">Periodo 3</th>
                <th class="col-nota">Periodo 4</th>
                <th class="col-nota" style="background-color: #e2e8f0;">Acumulado</th>
                <th class="col-nota" style="background-color: #e2e8f0;">Promedio</th>
            </tr>
        </thead>
        <tbody>
            {{#each estudiantes}}
            <tr>
                <td class="col-nro">{{this.numero}}</td>
                <td class="col-nombre">{{this.nombreCompleto}}</td>
                <td class="col-nota">{{this.p1}}</td>
                <td class="col-nota">{{this.p2}}</td>
                <td class="col-nota">{{this.p3}}</td>
                <td class="col-nota">{{this.p4}}</td>
                <td class="col-nota" style="font-weight: bold;">{{this.acumulado}}</td>
                <td class="col-nota" style="font-weight: bold; color: #1e40af;">{{this.promedio}}</td>
            </tr>
            {{/each}}
        </tbody>
    </table>

</body>

</html>